<!DOCTYPE html>
<html>
<head>
    <title>Admin Dashboard</title>
</head>
<body>
<h2>Admin Dashboard</h2>
<p>Logged in as <%= user.email %></p>
<h3>Admins</h3>
<ul>
<% (allowed.admins || []).forEach(function(e){ %>
<li><%= e %></li>
<% }); %>
</ul>
<h3>Users</h3>
<ul>
<% (allowed.users || []).forEach(function(e){ %>
<li><%= e %></li>
<% }); %>
</ul>
<form method="post" action="/admin/add-email">
    <input type="email" name="email" placeholder="Email" required>
    <button type="submit">Add</button>
</form>
<p><a href="/logout">Logout</a></p>
</body>
</html>
